package com.lusna.ffi.deser;

import java.util.Optional;

public enum DomainSpecificResponseType {
    GetActiveSessions,
    GetAccounts,
    PeerList,
    Register,
    Connect,
    Disconnect
}