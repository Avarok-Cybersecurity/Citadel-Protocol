Clang 13+ is required to compile oqs + liboqs

Install [rust](https://www.rust-lang.org/tools/install).

Make sure to run "cargo clean" before recompiling using the new version of clang

To install it, run "brew install llvm@13" or "arch -arm64 brew install llvm@13"

Install cmake: `brew install cmake`