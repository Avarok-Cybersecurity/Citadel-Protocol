/*use crate::nat_identification::NatType;

pub struct MultiDeltaUdpHolePuncher {

}

impl MultiDeltaUdpHolePuncher {
    /// Optionally returns self. This mode will only be used when one or more nat config have deltas in the predicted ports
    pub async fn new(local_nat_type: &NatType, peer_nat_type: &NatType, breadth: usize) -> Result<Option<Self>, anyhow::Error> {
        let (access_local, access_peer) = local_nat_type.traversal_type_required_with(peer_nat_type);

    }
}
*/