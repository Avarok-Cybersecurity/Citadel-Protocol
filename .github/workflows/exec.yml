name: Execute Pipeline
on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize
  workflow_dispatch:
jobs:
  workflow:
    name: Unit tests
    uses: ./.github/workflows/workflow.yml