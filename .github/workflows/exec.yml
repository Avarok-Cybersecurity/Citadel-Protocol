name: Execute Pipeline
on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize
  workflow_dispatch:
jobs:
  workflow:
    uses: ./.github/workflows/workflow.yml